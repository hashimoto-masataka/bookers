
<div>
    <%= flash[:notice] %>
</div>
<table>
<h1>Books</h1>
  <nav>
   <ul class="table-name">
      <li class="table-title">Title</li>
      <li class="table-body">Body</li>
      <li class="table-link"></li>
    </ul>
  </nav>
<div class ="index">
  <%@books. each do |book| %>
<div class="table-list">
  <div class="table-title-item">
   <span><%= book.title %></span>
  </div>
  <div class="table-body-item">
   <span><%=book.body %></span>
  </div>
<div class="submit">
 <tb><%= link_to "Show" , book_path(book.id) %></tb>
 <tb><%= link_to "Edit", edit_book_path(book.id) %></tb>
 <tb><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure?？" %>
  </tb> 
</div></div>

<% end %>
</table>

<h2>New book</h2>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>


<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <h4><%= f.submit 'Create Book' %></h4>
<% end %>

</div>
